#!/usr/bin/env sh

set -eu

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

git config core.hooksPath .githooks

printf 'configured repo-managed Git hooks at %s/.githooks\n' "$repo_root"
